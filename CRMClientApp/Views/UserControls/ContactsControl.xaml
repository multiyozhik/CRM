<UserControl x:Class="CRMClientApp.Views.UserControls.ContactsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top">
                <TextBlock Text="{Binding}"/>
                <TextBlock Text="Телефон: {Binding}"/>
                <TextBlock Text="Факс: {Binding}"/>
                <TextBlock Text="Email: {Binding}"/>
                <TextBlock Text="{Binding}"/>
            </StackPanel>
            <WrapPanel DockPanel.Dock="Bottom">
                <Image Source="{Binding}" Width="Auto" Height="Auto"/>
                <Image Source="{Binding}" Width="Auto" Height="Auto"/>
            </WrapPanel>
        </DockPanel>
        <Image Grid.Column="1" Source="{Binding}" Width="Auto" Height="Auto"/>
    </Grid>
</UserControl>
